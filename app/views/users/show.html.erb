<p style="color: green"><%= notice %></p>

<div class="d-flex flex-column align-items-center">

<%= render @user %>

  <div class="d-flex">
    <%= link_to "Edit this user", edit_user_registration_path(@user), class: "btn btn-primary me-3" %> 

    <%= button_to "Destroy this user", user_registration_path(@user), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger" %>
  </div>
</div>
<%= link_to "Back", root_path, class: "btn btn-primary" %>

